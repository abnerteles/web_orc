{
  "framework": "nextjs",
  "buildCommand": "npm install --save-dev @types/react @types/node && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "env": {
    "SKIP_ENV_VALIDATION": "1",
    "NEXT_PUBLIC_FORCE_DEPLOYMENT": "true"
  }
}